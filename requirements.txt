# Core
torch>=2.5.0
transformers>=4.50.0
numpy>=1.26.0
Pillow>=10.0.0
tqdm>=4.66.0

# Data / metrics
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.13.0

# Vision-language utils
qwen-vl-utils>=0.0.10        # Qwen2-VL / Qwen2.5-VL preprocessing
bitsandbytes>=0.43.0         # 4-bit quantisation (optional)
flash-attn>=2.6.0            # Flash attention 2 (optional, build can fail on some systems)

# Description-task NLP metrics
nltk>=3.9                     # BLEU, METEOR
rouge-score>=0.1.2            # ROUGE
bert-score>=0.3.13            # BERTScore

# Plotting / paper assets
matplotlib>=3.8.0
seaborn>=0.13.0
umap-learn>=0.5.6             # 2-D projections (optional; falls back to PCA)

# LLM-as-judge dataset validation
anthropic>=0.30.0

# Presentation builder
python-pptx>=0.6.23
